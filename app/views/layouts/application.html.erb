<!DOCTYPE html>
<html data-bs-theme="dark">
  <head>
    <title>Neomonastery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <nav class="navbar navbar-dark bg-dark d-flex justify-content-between">
    <a href="<%= root_path %>" class="navbar-brand">Neo Monastery</a>
    <div>
      <% if user_signed_in? %>
        <%= link_to 'Logout', destroy_user_session_path, class: 'btn btn-danger',  data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }  %>
    <% else %>
        <a class="btn btn-primary me-2" href="<%= new_user_session_path %>">Login</a>
        <a class="btn btn-success" href="<%= new_user_registration_path %>">Register</a>
      <% end %>

    </div>

  </nav>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>
  </body>
</html>
